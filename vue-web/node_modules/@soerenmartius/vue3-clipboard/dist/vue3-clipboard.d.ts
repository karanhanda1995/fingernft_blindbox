import { App } from 'vue';

declare type Actions = 'copy' | 'cut';

declare interface ClipboardConfig {
    autoSetContainer: boolean;
    appendToBody: boolean;
}

declare interface IVueClipboard {
    /**
     * Set global config
     */
    config(config: ClipboardConfig): void;
    /**
     * Copy string to clipboard
     */
    toClipboard(text: string, action: Actions): Promise<unknown>;
    install(app: App): void;
}

export declare const toClipboard: (text: string, action?: Actions) => Promise<unknown>;

export declare const VueClipboard: IVueClipboard;

export { }
